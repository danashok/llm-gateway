services:
  app:
    build:
      context: ../
      dockerfile: Dockerfile
    container_name: llm-gateway_app
    ports:
      - "8080:8080"
    env_file:
      - ../.env
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
